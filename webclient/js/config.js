(function (mypass) {
  'use strict';

  mypass.Events = {
      APP_NAV:{
        nav:'appnav',
        signup:'nav.signup',
        login:'nav.login',
        dashboard:'nav.main'
      }
  };

  function init() {
    // window.addEventListener('onLoginLoad', onLoginLoad);
  }

  init();

})(mypass);